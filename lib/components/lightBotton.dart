import 'package:flutter/material.dart';

class DayNightBotton extends StatelessWidget {
  const DayNightBotton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialButton(
      onPressed: () {},
    );
  }
}
